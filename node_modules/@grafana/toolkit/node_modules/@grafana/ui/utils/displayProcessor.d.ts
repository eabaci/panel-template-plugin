import { Threshold, FieldConfig, DisplayProcessor, DecimalInfo, DecimalCount } from '@grafana/data';
import { GrafanaTheme } from '../types/index';
interface DisplayProcessorOptions {
    field?: FieldConfig;
    isUtc?: boolean;
    theme?: GrafanaTheme;
}
export declare function getDisplayProcessor(options?: DisplayProcessorOptions): DisplayProcessor;
export declare function getColorFromThreshold(value: number, thresholds: Threshold[], theme?: GrafanaTheme): string;
export declare function getDecimalsForValue(value: number, decimalOverride?: DecimalCount): DecimalInfo;
export {};
