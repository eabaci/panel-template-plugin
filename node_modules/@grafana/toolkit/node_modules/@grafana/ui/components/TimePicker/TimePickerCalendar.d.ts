import { PureComponent } from 'react';
import { TimeFragment, TimeZone } from '@grafana/data';
import { DateTime } from '@grafana/data';
export interface Props {
    value: TimeFragment;
    roundup?: boolean;
    timeZone?: TimeZone;
    onChange: (value: DateTime) => void;
}
export declare class TimePickerCalendar extends PureComponent<Props> {
    onCalendarChange: (date: Date | Date[]) => void;
    onDrilldown: (props: any) => void;
    render(): JSX.Element;
}
