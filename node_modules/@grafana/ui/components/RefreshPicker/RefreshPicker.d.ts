import { PureComponent } from 'react';
import { SelectableValue } from '@grafana/data';
export declare const offOption: {
    label: string;
    value: string;
};
export declare const liveOption: {
    label: string;
    value: string;
};
export declare const defaultIntervals: string[];
export declare const isLive: (refreshInterval: string) => boolean;
export interface Props {
    intervals?: string[];
    onRefresh: () => any;
    onIntervalChanged: (interval: string) => void;
    value?: string;
    tooltip: string;
    hasLiveOption?: boolean;
}
export declare class RefreshPicker extends PureComponent<Props> {
    constructor(props: Props);
    intervalsToOptions: (intervals: string[] | undefined) => SelectableValue<string>[];
    onChangeSelect: (item: SelectableValue<string>) => void;
    render(): JSX.Element;
}
