import { DataFrame, NullValueMode } from '../types';
import { Registry, RegistryItem } from './registry';
export declare enum ReducerID {
    sum = "sum",
    max = "max",
    min = "min",
    logmin = "logmin",
    mean = "mean",
    last = "last",
    first = "first",
    count = "count",
    range = "range",
    diff = "diff",
    delta = "delta",
    step = "step",
    firstNotNull = "firstNotNull",
    lastNotNull = "lastNotNull",
    changeCount = "changeCount",
    distinctCount = "distinctCount",
    allIsZero = "allIsZero",
    allIsNull = "allIsNull"
}
export interface FieldCalcs {
    [key: string]: any;
}
declare type FieldReducer = (data: DataFrame, fieldIndex: number, ignoreNulls: boolean, nullAsZero: boolean) => FieldCalcs;
export interface FieldReducerInfo extends RegistryItem {
    emptyInputResult?: any;
    standard: boolean;
    reduce?: FieldReducer;
}
interface ReduceFieldOptions {
    series: DataFrame;
    fieldIndex: number;
    reducers: string[];
    nullValueMode?: NullValueMode;
}
/**
 * @returns an object with a key for each selected stat
 */
export declare function reduceField(options: ReduceFieldOptions): FieldCalcs;
export declare const fieldReducers: Registry<FieldReducerInfo>;
export {};
