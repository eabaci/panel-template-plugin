import { DataFrame, TimeSeries, FieldType, TableData } from '../types/index';
/**
 * Given a value this will guess the best column type
 *
 * TODO: better Date/Time support!  Look for standard date strings?
 */
export declare function guessFieldTypeFromValue(v: any): FieldType;
/**
 * Looks at the data to guess the column type.  This ignores any existing setting
 */
export declare function guessFieldTypeFromSeries(series: DataFrame, index: number): FieldType | undefined;
/**
 * @returns a copy of the series with the best guess for each field type
 * If the series already has field types defined, they will be used
 */
export declare const guessFieldTypes: (series: DataFrame) => DataFrame;
export declare const isTableData: (data: any) => data is DataFrame;
export declare const isDataFrame: (data: any) => data is DataFrame;
export declare const toDataFrame: (data: any) => DataFrame;
export declare const toLegacyResponseData: (series: DataFrame) => TableData | TimeSeries;
export declare function sortDataFrame(data: DataFrame, sortIndex?: number, reverse?: boolean): DataFrame;
